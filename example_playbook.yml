---
- name: A play that sources stats from previous play in workflow
  hosts: localhost
  tasks:
    - debug:
        msg: "IPs from previous play are {{ item[0].public_ip }}"
      with_items: "{{ systems.instances }}"
